<Window x:Class="WPF_CNC_Simulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_CNC_Simulator"
        xmlns:widgets="clr-namespace:WPF_CNC_Simulator.Vistas.Widgets"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280"
        MinHeight="600" MinWidth="800">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.125*"/>
            <RowDefinition Height="0.825*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            
        </Grid.ColumnDefinitions>

        <widgets:MenuSuperior Grid.Row="0" Grid.Column="0"/>
        <Button Content="🔄 Slicer STL → G-code" 
            Click="OpenSlicerWindow_Click"
            Width="180"
            Height="35"
                />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.66*"/>
                <ColumnDefinition Width="0.33*"/>
            </Grid.ColumnDefinitions>

            
            <widgets:EditorGCode Grid.Row="0" Grid.Column="1"/>
            <widgets:Simulador3d Grid.Row="0" Grid.Column="0" x:Name="Simulador3d"/>
        </Grid>
        
       

    </Grid>
</Window>
