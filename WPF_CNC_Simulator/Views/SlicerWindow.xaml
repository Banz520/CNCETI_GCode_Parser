<Window x:Class="WPF_CNC_Simulator.Views.SlicerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WPF_CNC_Simulator.ViewModels"
        mc:Ignorable="d"
        Title="Conversor STL a G-code" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.DataContext>
        <vm:SlicerViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <Border Grid.Row="0" Background="#0078D7" Padding="15" Margin="0,0,0,20" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="🔄 Conversor de Modelos 3D" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Transforma archivos STL en G-code para tu impresora 3D" 
                           FontSize="13" 
                           Foreground="#E0E0E0"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Archivo de entrada -->
        <GroupBox Grid.Row="1" Header="📁 Archivo de Entrada (STL)">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                         Text="{Binding STLFilePath, UpdateSourceTrigger=PropertyChanged}"
                         IsReadOnly="True"
                         Background="#FAFAFA"
                         ToolTip="Selecciona un archivo STL para convertir"/>

                <Button Grid.Column="1" 
                        Content="📂 Seleccionar STL"
                        Command="{Binding SelectSTLCommand}"
                        Width="160"/>
            </Grid>
        </GroupBox>

        <!-- Archivo de salida -->
        <GroupBox Grid.Row="2" Header="💾 Archivo de Salida (G-code)">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                         Text="{Binding GCodeOutputPath, UpdateSourceTrigger=PropertyChanged}"
                         Background="#FAFAFA"
                         ToolTip="Ruta donde se guardará el G-code generado"/>

                <Button Grid.Column="1" 
                        Content="💾 Guardar como..."
                        Command="{Binding SelectOutputCommand}"
                        Width="160"/>
            </Grid>
        </GroupBox>

        <!-- Configuración -->
        <GroupBox Grid.Row="3" Header="⚙️ Configuración de Impresión">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Fila 1 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="🔺 Altura de capa (mm):" FontWeight="SemiBold"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         Text="{Binding Settings.LayerHeight, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Altura de cada capa (típico: 0.1-0.3mm)"/>

                <TextBlock Grid.Row="0" Grid.Column="3" Text="📊 Densidad (%):"/>
                <TextBox Grid.Row="0" Grid.Column="4" 
                         Text="{Binding Settings.FillDensity, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Porcentaje de relleno interno (0-100%)"/>

                <TextBlock Grid.Row="0" Grid.Column="6" Text="🔄 Perímetros:"/>
                <TextBox Grid.Row="0" Grid.Column="7" 
                         Text="{Binding Settings.Perimeters, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Número de paredes externas"/>

                <!-- Fila 2 -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="⚡ Velocidad (mm/s):" FontWeight="SemiBold"/>
                <TextBox Grid.Row="2" Grid.Column="1" 
                         Text="{Binding Settings.PrintSpeed, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Velocidad de impresión"/>

                <TextBlock Grid.Row="2" Grid.Column="3" Text="🌡️ Temp. extrusor (°C):"/>
                <TextBox Grid.Row="2" Grid.Column="4" 
                         Text="{Binding Settings.ExtruderTemperature, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Temperatura del extrusor"/>

                <TextBlock Grid.Row="2" Grid.Column="6" Text="🔥 Temp. cama (°C):"/>
                <TextBox Grid.Row="2" Grid.Column="7" 
                         Text="{Binding Settings.BedTemperature, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Temperatura de la cama caliente"/>

                <!-- Fila 3 -->
                <CheckBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                          Content="🏗️ Generar soportes"
                          IsChecked="{Binding Settings.SupportMaterial}"
                          FontWeight="SemiBold"
                          VerticalAlignment="Center"
                          ToolTip="Agregar estructuras de soporte para voladizos"/>
            </Grid>
        </GroupBox>

        <!-- Área de progreso -->
        <GroupBox Grid.Row="4" Header="📊 Progreso y Estado">
            <Border BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    Background="#FAFAFA" 
                    CornerRadius="3">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding ProgressText}" 
                               TextWrapping="Wrap"
                               FontFamily="Consolas"
                               FontSize="11"
                               Margin="10"
                               Foreground="#333333"/>
                </ScrollViewer>
            </Border>
        </GroupBox>

        <!-- Botón de conversión -->
        <Button Grid.Row="5" 
                Content="🚀 CONVERTIR A G-CODE"
                Command="{Binding SliceCommand}"
                FontSize="16" 
                FontWeight="Bold"
                Height="55"
                Background="#4CAF50"
                Margin="0,10,0,0">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#4CAF50"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="5"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#CCCCCC"/>
                            <Setter Property="Foreground" Value="#666666"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#45A049"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#3D8B40"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>